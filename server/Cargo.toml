[package]
name = "oxide"
version = "0.1.0"
edition = "2024"

[dependencies]
lib = {path = "../lib", version = "*"}
data = {path = "../data", version = "*"}
flate2 = "1.1.2"

[profile.dev]
opt-level = 1
